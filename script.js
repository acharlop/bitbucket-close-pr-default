// get checkbox
var checkbox = document.getElementById("id_close_anchor_branch")

// if checkbox exists and it is not checked, check it
if (checkbox && !checkbox.checked) {
    checkbox.checked = true
}